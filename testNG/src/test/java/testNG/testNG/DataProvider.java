package testNG.testNG;

public class DataProvider {
	
	//Data providers are used for data driven testing, same test can be run with different set of data.
	//we have to create a method for providing data to test methods
	//Syntax==
	//@Data Provider(name="name of the data provider")
	//public object[][] dataProviderMethod(){
		//return the object[][]//always return the array of objects
	//{values}; }

	
}
